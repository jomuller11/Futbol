type Message {
  id: ID!
  content: String!
}

type Query {
  getMessage(id: ID!): Message
}

type Mutation {
  createMessage(id: ID!, content: String!): Message
}

schema {
  query: Query
  mutation: Mutation
}
